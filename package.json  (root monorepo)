{
  "name": "@verdex/monorepo",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently -c auto \"yarn workspace @verdex/api dev\" \"yarn workspace @verdex/mobile start\"",
    "build": "yarn workspaces foreach -pt run build",
    "lint": "yarn workspaces foreach -pt run lint",
    "test": "yarn workspaces foreach -pt run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
